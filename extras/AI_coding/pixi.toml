[workspace]
name = "AI_coding"
version = "0.1.0"
description = "AI coding experiments"
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]

[dependencies]
python = ">=3.12"

[pypi-dependencies]
