# Extras

Side projects and experiments. They are **not** part of the root CMake super-build in the repo root. See **Extras** in [../GUIDE.md](../GUIDE.md).

Use a local `build/` (or your own layout) per project; those directories should stay untracked.
