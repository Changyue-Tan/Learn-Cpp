[workspace]
name = "latency"
version = "0.1.0"
description = "Stress test latency benchmarks and plotting"
channels = ["conda-forge"]
platforms = ["linux-64"]

[tasks]
bench = "python stress_tiers.py"

[dependencies]
python = ">=3.12"

[pypi-dependencies]
matplotlib = ">=3.5"
pandas = ">=1.3"
