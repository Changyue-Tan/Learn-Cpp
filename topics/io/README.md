# I/O and scheduling

Token bucket, timer wheel, nonblocking TCP echo, epoll/kqueue event loop, async logger. Several demos assume a POSIX-like environment.

See [../../GUIDE.md](../../GUIDE.md) (pillar index → I/O).
